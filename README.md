Hello this is my vimrc configuration

In order to install it your will have to do the following:

* git clone git://github.com/kokeroulis/vimrc.git ~/vimrc
* cd ~/vimrc
* cp vimrc ~/.vimrc
* cp -r vim ~/.vim
